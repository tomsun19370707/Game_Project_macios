# 🍎 iOS 端原生游戏移植核心玩法细则 (开发对齐专用)

本细则针对 iOS 团队提出的玩法交互流转、高级兑换保底机制以及自定义钥匙购买等核心疑问，提供了 Android 端的实际落地实现方案和业务逻辑细节，以确保两端在功能和体验上 100% 对齐。

---

## 🎯 一、 玩法2（神木栖灵）9个灵果（桃子）的交互状态流转

### 1. 灵果交互作用确认 (无点击手势)
* **规则**：大树上定位挂载的 9 个灵果（ImageView）**纯粹作为当前奖池礼物的“静态展示挂件”**。
* **对齐**：用户**无法直接点击**树上的任意灵果来触发单抽。所有的抽奖交互均通过底部的“祝灵1次”、“祝灵10次”、“祝灵100次”按钮发起。

### 2. 图片动态加载与渲染
* **初始化**：页面 `onCreate` 并发起 `/api/emo/lottery/get_prizes` 奖池请求。拿到返回的 9 个奖品列表后，调用 Glide/SDWebImage 将这 9 个奖品的**真实礼物图片**（由 pic/image 字段返回）动态渲染到大树对应的 9 个灵果控件上，它们就像树上的果实一样悬挂展示。

### 3. 动画与状态切换 (极简高保真)
* **注意**：iOS 团队解压到的 `theme_game_two_center_fruit.png` 和 `theme_game_two_center_frame.png` 是最早预览壳子时使用的旧版居中果实和边框。**在第二阶段视觉调整后，客户明确要求移除了这两个居中控件**。
* **流转状态**：抽奖时，9 个灵果无复杂的成熟、发光、采摘动画。点击祝灵后，本地乐观扣钥匙，立即起播全屏的 SVGA 动效（`theme_game_two_draw.svga`）进行视觉遮挡；动效结束后直接弹出结果弹窗，关闭弹窗时重载主页，保持果实纯静态展示。

---

## 🎯 二、 玩法1（寻梦之旅）高级兑换与保底机制

### 1. 兑换弹窗中藏宝图投入数量选择 (无加减号的循环点击交互)
* **背景**：因为 UI 布局上没有空间去绘制加减号输入器，Android 落地了一个非常巧妙好用的循环调节交互：
* **实现**：
  * 将弹窗右侧展示“X张/去拥有”的藏宝图文本标签设为 **可点击按钮**。
  * 用户点击该文本时，投入的藏宝图数量 `card_count` 在 **`0` 到 `当前用户最大拥有的藏宝图数量`** 之间循环切换。
  * *例如*：用户当前拥有 3 张藏宝图。点击文本：投入 `0` 张 ──► 投入 `1` 张 ──► 投入 `2` 张 ──► 投入 `3` 张 ──► 回到 `0` 张。
  * 随着张数变化，投入成功率实时刷新：`成功率 = min(100, 单张成功率 * card_count)`。

### 2. 寻梦值（保底值）展示与更新
* **展示区域**：寻梦值展示在主页面底端上方的**蓝色进度条**（ProgressBar）以及其旁边的 **`currentLucky/maxLucky` 文本**上（范围 0~200）。
* **更新动画**：每次抽奖结束后，进度条的进度和文本数字随接口返回的最新 `luckyValue` 动态增长（无需特殊粒子动效，进度条拉长即可）。
* **保底触发**：当寻梦值达到 `200` 时，下一次抽奖会在本地触发保底判定（寻梦值重置为 0，跑马灯强制定格在第 1 格大奖，抽奖结束时弹出“恭喜触发寻梦保底”提示）。

### 3. 手动刷新 18格奖池动画
* **规则**：点击“刷新奖池”按钮发起 `/api/emo/lottery/refresh_pool` 接口。
* **对齐**：数据成功返回后，直接更新 18 格子的礼物配置即可，**无需播放卡牌翻转或水波纹动效**。因为图片框架在重新 load 网络图片时，自带淡入淡出（CrossFade）加载过渡，视觉上已经足够平滑。

---

## 💰 三、 购买弹窗中的“其他数量”自定义购买校验 (防崩防刷)

当用户在购买钥匙弹窗（`PurchaseDialog`）中点击了“其它”选项时：

### 1. 自定义输入框展示
* **交互**：点击“其它”按钮时，在购买弹窗的中间区域动态展示（`hidden = NO`）一个原生的输入框（`UITextField`）。

### 2. 防刷与空校验红线 (Security Rules)
* **纯数字限制**：输入框必须设置 `keyboardType = UIKeyboardTypeNumberPad`，只允许输入正整数，过滤所有符号、小数点及英文字母。
* **防空/零拦截**：点击“确认购买”时，如果输入框为空、不填字符或输入为 `0`，**直接阻断请求**，并弹出 Toast 提示：*“请输入购买数量”*。
* **最大值上限拦截**：限制单次自定义购买的钥匙最大数量为 **`9999`** 个。若输入值大于 `9999`，弹出提示：*“单次购买不能超过 9999 个”* 并拦截。
* **实时消耗预算计算**：监听输入框的文本变化（`EditingChanged`），输入时动态计算并刷新弹窗底部确认按钮上的文字：
  $$\text{需要钻石} = \text{输入数量} \times \text{单把钥匙价格(通常为200钻石)}$$
