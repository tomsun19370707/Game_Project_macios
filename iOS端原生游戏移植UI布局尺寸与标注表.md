# 🍎 iOS 端原生游戏移植 UI 布局尺寸与标注表 (AI 专属喂料)

针对 AI 助手在开发 iOS 端（Objective-C）UI 界面时，可能会因为“视觉感官误差”导致布局跑偏、控件大小失真等问题，本标注表直接提取了 Android 端已通过 100% 真机验收的**高保真尺寸、物理绝对坐标以及相对间距比例**。

> [!TIP]
> **如何使用本表**：
> 请直接将本标注表作为“提示词（Prompt）”或者“参考上下文”输入给负责编写 iOS 代码的 AI，让其依据表内的数据使用 `Masonry` 或 `Frame` 布局进行绝对值绑定，无需通过视觉瞎猜。

---

## 🎨 一、 玩法2（神木栖灵）高精度定位与尺寸参数

### 1. 核心背景大图基准
* **原始设计大图分辨率**：$750 \times 1311$ 像素（Android `xxhdpi`，等价于 iOS `@3x` 规格）。
* **UIImageView 填充模式**：必须设为 `.scaleAspectFit`，以保证全机型大图不变形。

### 2. 9个灵果（桃子）的绝对定位与大小
在 $750 \times 1311$ 设计稿基准下，9 个灵果 ImageView 的中心点绝对坐标（设计值）和它的设计大小（PEACH_SIZES，dp单位）如下：

| 灵果序号 | 目标中心点设计 X 坐标 | 目标中心点设计 Y 坐标 | 原始设计大小 (宽度 = 高度) |
| :--- | :--- | :--- | :--- |
| **灵果 1** | `385.0` | `330.0` | `105` pt |
| **灵果 2** | `205.0` | `450.0` | `75` pt |
| **灵果 3** | `585.0` | `460.0` | `75` pt |
| **灵果 4** | `425.0` | `560.0` | `60` pt |
| **灵果 5** | `100.0` | `610.0` | `65` pt |
| **灵果 6** | `635.0` | `695.0` | `70` pt |
| **灵果 7** | `270.0` | `680.0` | `60` pt |
| **灵果 8** | `130.0` | `760.0` | `65` pt |
| **灵果 9** | `470.0` | `830.0` | `80` pt |

* **灵果视图额外缩放系数**：由于切图边缘有半透明呼吸阴影，最终代码渲染时的实际控件大小需在上述“原始设计大小”基础上乘上缩放系数 **`0.6f`**（即 `targetSize = PEACH_SIZE * scale * 0.6`）。

### 3. 主面板其他控件定位 (Masonry 约束参考)
* **今日运势悬浮条 (`tv_fortune`)**：居中偏右。在背景大图外侧偏上方挂载，高度 `32` pt，左右内边距 `12` pt。
* **规则按钮 (`iv_rule`) 与记录按钮 (`iv_record`)**：
  * 大小均为 `36 * 36` pt。
  * 规则按钮定位在背景大图左上角，距顶部 `16` pt，距左侧 `16` pt。
  * 记录按钮在右侧对称定位，距顶部 `16` pt，距右侧 `16` pt。
* **充值加号 (`iv_plus`)**：位于钻石余额条的右侧，点击热区大小 `24 * 24` pt。

---

## 🎨 二、 玩法1（寻梦之旅）UI 布局与约束参数

### 1. 18宫格礼物轮盘 (环形排布)
* 每一个礼物卡片（`FrameLayout` 等效的 UIView）大小固定为：宽 `46` pt，高 `60` pt。
* **排布约束**：18 个格子按顺时针组成扁平环状：
  * **顶部横排 6 个**：格子 1 ~ 6。
  * **右侧竖排 2 个**：格子 7 ~ 8。
  * **底部横排 6 个**：格子 9 ~ 14。
  * **左侧竖排 2 个**：格子 15 ~ 18。
* 格子之间的水平间距设为 `8` pt，垂直间距为 `6` pt。

### 2. 底部抽奖按钮组 (三角形“品”字排布)
* 三个抽奖按钮大小统一为：宽 `112` pt，高 `56` pt。
* **品字形排布**：
  * **置顶居中**：“十运齐聚”（10连抽，对应 ID `iv_draw_ten`）。
  * **左下分布**：“一星纳福”（1抽，对应 ID `iv_draw_one`），位于十连抽按钮的左下方，间距为 `12` pt。
  * **右下分布**：“百祥落盘”（100抽，对应 ID `iv_draw_hundred`），位于十连抽的右下方，间距为 `12` pt。

---

## 🎨 三、 弹窗通用尺寸参数表 (Popups)

为了保证 iOS 端弹窗展现比例的优雅度，各子弹窗建议使用固定 Frame 或 Masonry 约束限制宽高：

### 1. 购买钥匙弹窗 (PurchaseDialog)
* **弹窗背景外框大小**：宽 `315` pt，高 `360` pt。
* **关闭按钮 (`iv_purchase_close`)**：大小为 `36 * 36` pt，位于右上角（距顶部 `16` pt，距右侧 `16` pt）。
* **档位选择按钮 (1/10/100/其它)**：大小为 `72` pt × `34` pt，贴在弹窗底部上方，按钮相互间距 `8` pt。
* **确认购买按钮 (`iv_purchase_confirm`)**：大小为 `260` pt × `86` pt，居中贴在弹窗最底部，向下偏移 `10` pt 以配合素材质感。

### 2. 恭喜获得中奖结果弹窗 (ResultDialog)
* **弹窗背景外框大小**：宽 `315` pt，高 `470` pt（瘦长高雅型设计，以保证去重列表有足够的滚动展示空间）。
* **再抽一次按钮 (`tv_result_retry`)**：大小为宽 `160` pt，高 `32` pt，居中位于弹窗底部上方。
* **次数提示标签 (`layout_result_tag_container`)**：大小为宽 `120` pt，高 `19` pt，居中位于“再抽一次”按钮的下方，距弹窗底部 `20` pt。
