# 新原生游戏（寻梦之旅 & 神木栖灵）后端接口对接指南
> **后端接口已确认/改造版 | 2026-06-18**

> [!NOTE]
> **开发背景**  
> 本次新游戏（玩法1：寻梦之旅，玩法2：神木栖灵）是在 Android 客户端采用**原生高保真 UI** 进行重构。  
> 经排查与对接联调，核心业务逻辑全面复用和改造原有的后端抽奖接口，最新一次的接口改动与重构在 2026-06-18 完成了联调适配。

---

## 📅 一、 基础对接规范与改动摘要

1. **响应状态码**
   * 后端当前成功响应统一使用：`code: 1`（**注意：成功码为 1，非 200**）。
   * 失败响应通常为：`code: 0`。
   * 未登录/登录失效响应为：`code: 401`。
   * Token 传递方式：通过请求头 `token` 或请求参数 `token` 传递。

2. **图片资源适配**
   * 后端图片字段现在同时返回 `image` 和 `pic`，Android 客户端可**优先使用 `pic`** 字段展示。

3. **玩法与 ID 映射**
   * 后端通过 `type_id` 区分不同玩法。后端数据库目前没有强制更改玩法名称，仍沿用原名（例如“神域契约”等）。
   * 客户端在调用时应传递对应的玩法 `type_id` 进行过滤或请求（玩法1寻梦之旅 ID=3，玩法2神木栖灵 ID=4）。

---

## 📅 二、 核心复用接口详情

### 1. 查询个人余额
*   **接口 URL**: `/api/emo/user/getMoney` (POST)
*   **请求头**:
    *   `token` (是): 登录 token
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "请求成功",
      "data": {
        "money": "0.00",
        "diamond": "100.00",
        "lottery_coin": 1000,
        "ratio_coin": 0,
        "prize_coin": 0
      }
    }
    ```
*   **前端使用**:
    *   钻石余额：`data.diamond`（**注意：数据类型为字符串**）
    *   钥匙/抽奖代币余额：`data.lottery_coin`（数字）

### 2. 查询玩法详情和钥匙余额
*   **接口 URL**: `/api/emo/lottery/get_room_detail` (POST)
*   **请求参数**:
    *   `id` (是, int): 抽奖盘 ID（对应 `type_id`）
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "获取成功",
      "data": {
        "id": 3,
        "name": "神域契约",
        "image": "https://...png",
        "pic": "https://...png",
        "content": "",
        "coin_cost_opt": [
          {"nums": 1, "coin_cost": 200},
          {"nums": 10, "coin_cost": 2000},
          {"nums": 20, "coin_cost": 4000}
        ],
        "lottery_coin": 1591642
      }
    }
    ```
*   **前端使用**:
    *   玩法图：`data.pic`
    *   当前钥匙余额：`data.lottery_coin`
    *   抽奖按钮档位：`data.coin_cost_opt`（`nums` 和 `coin_cost` 均为数字。抽奖时参数 `times` 传对应 `nums`，不可前端写死 1/10/100）

### 3. 获取奖池奖品列表
*   **接口 URL**: `/api/emo/lottery/get_prizes` (POST)
*   **请求参数**:
    *   `type_id` (是, int): 抽奖盘 ID
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "获取成功",
      "data": [
        {
          "id": 134,
          "name": "s—抽签",
          "image": "https://...png",
          "pic": "https://...png",
          "nature": "real",
          "is_grand": 0,
          "price": 1000,
          "value": 0,
          "nums": 2491
        }
      ]
    }
    ```
*   **前端使用**:
    *   礼物 ID：`id`，礼物名：`name`，图片：`pic`，钻石价值：`price`（数字），剩余库存：`nums`

### 4. 执行抽奖
*   **接口 URL**: `/api/emo/lottery/draw` (POST)
*   **请求参数**:
    *   `type_id` (是, int): 抽奖盘 ID
    *   `times` (否, int): 抽奖次数，默认 1（必须是 `coin_cost_opt` 配置中的 `nums` 之一）
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "抽奖结束",
      "data": {
        "list": [
          {
            "id": 133,
            "name": "s—捕梦网",
            "image": "https://...png",
            "pic": "https://...png",
            "nature": "real",
            "is_grand": 0,
            "price": 2000,
            "num": 1
          }
        ],
        "nums": 1,
        "total_value": 2000,
        "lottery_log_id": 9673,
        "profit_info": {
          "total_cost": 200,
          "total_income": 2000,
          "profit": 1800,
          "profit_rate": 1000,
          "consume_diamonds": 200,
          "produce_diamonds": 2000
        }
      }
    }
    ```
*   **前端使用**:
    *   中奖列表：`data.list`
    *   中奖总价值：`data.total_value`
    *   中奖汇总日志 ID：`data.lottery_log_id`
    *   **跑马灯落点定位**：按 `data.list` 中 `price` 最高的礼物 ID 作为落点进行转盘定位。

### 5. 钻石购买钥匙与棒棒糖发包
*   **接口 URL**: `/api/emo/user/diamondChangeLotteryCoin` (POST)
*   **请求参数**:
    *   `diamond` (是, number): 消耗钻石数量
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "兑换成功",
      "data": {
        "coin": 1,
        "lollipop_gift_id": 107,
        "lollipop_num": 1
      }
    }
    ```
*   **前端使用**:
    *   获得钥匙数：`data.coin`，发放的棒棒糖 ID：`data.lollipop_gift_id`，发放数量：`data.lollipop_num`。
    *   说明：钻石兑换钥匙比例使用后台 `site.diamond_change_lottery_coin` 配置。

### 6. 获取玩法运势与收益率
*   **接口 URL**: `/api/fortune/getLotteryList` (GET)
*   **请求头**:
    *   `token` (是): 登录 token
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "获取成功",
      "data": [
        {
          "id": 3,
          "name": "神域契约",
          "image": "https://...png",
          "pic": "https://...png",
          "coin_cost": 10,
          "profit_rate": 1000,
          "consume_diamonds": 200,
          "produce_diamonds": 2000
        }
      ]
    }
    ```
*   **字段说明**:
    *   `profit_rate`: 运势收益率，计算公式为（今日产出 / 今日消耗）* 100（整型，例如 1000 表示 1000%），今日消耗为 0 时返回 0。
    *   `consume_diamonds`: 今日消耗/投入钻石数。
    *   `produce_diamonds`: 今日产出/中奖钻石价值。

### 7. 抽奖汇总记录 (折叠明细版)
*   **接口 URL**: `/api/emo/lottery/draw_log` (POST)
*   **请求参数**:
    *   `type_id` (是, int): 抽奖盘 ID
    *   `user_type` (否, string): `my` 我的记录；`all` 全服记录，默认 `all`
    *   `page` (否, int): 页码，默认 1
    *   `page_size` (否, int): 每页数量，默认 20
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "获取成功",
      "data": {
        "total": 239,
        "current_page": 1,
        "last_page": 239,
        "data": [
          {
            "id": 9673,
            "user_id": 1000170,
            "room_name": "神域契约",
            "draw_times": 1,
            "total_cost": 200,
            "create_time": "2026-06-17 18:59:37",
            "avatar": "https://...jpg",
            "nickname": "卡卡",
            "items": [
              {
                "id": 133,
                "name": "s—捕梦网",
                "image": "https://...png",
                "pic": "https://...png",
                "nature": "real",
                "is_grand": 0,
                "price": 2000,
                "num": 1
              }
            ],
            "total_value": 2000
          }
        ]
      }
    }
    ```
*   **前端使用**:
    *   一条记录代表一次抽奖请求（包含 1 抽、10 抽或 100 抽）。
    *   抽奖次数展示 `draw_times`，点击该项展开展示所获得的礼物列表 `items`。
    *   历史老数据因为在表结构中缺少 `lottery_log_id` 关联，其 `items` 可能为空，上线后的新数据支持精确展开。

### 8. 中奖记录列表 (跑马灯与全局大奖)
*   **接口 URL**: `/api/emo/lottery/win_log` (POST)
*   **请求参数**:
    *   `type_id` (是, int): 抽奖盘 ID
    *   `type` (否, string): `real` 仅真实礼物，默认 `real`
    *   `user_type` (否, string): `my` 我的记录；`all` 全服记录，默认 `all`
    *   `page` (否, int): 页码，默认 1
    *   `page_size` (否, int): 每页数量，默认 20
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "获取成功",
      "data": {
        "total": 2902,
        "current_page": 1,
        "data": [
          {
            "id": 179341,
            "lottery_log_id": 9673,
            "user_id": 1000170,
            "create_time": "2026-06-17 18:59:37",
            "name": "s—捕梦网",
            "image": "https://...png",
            "pic": "https://...png",
            "price": 2000,
            "nature": "real",
            "is_grand": 0,
            "avatar": "https://...jpg",
            "nickname": "卡卡"
          }
        ]
      }
    }
    ```

---

## 🆕 三、 后端新增/调整的特定接口

### 1. 玩法1：手动刷新 18 格奖池 (新增)
*   **接口 URL**: `/api/emo/lottery/refresh_pool` (POST)
*   **请求参数**:
    *   `type_id` (是, int): 抽奖盘 ID
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "刷新成功",
      "data": {
        "free_refresh_left": 0,
        "diamond_cost": 0,
        "diamond": 265660,
        "list": [
          {
            "id": 139,
            "name": "s—王者风范",
            "image": "https://...png",
            "pic": "https://...png",
            "nature": "real",
            "is_grand": 0,
            "price": 2880000,
            "value": 0,
            "nums": 1
          }
        ]
      }
    }
    ```
*   **前端使用**:
    *   `data.list` 固定返回刷新后的 18 个格子的礼物配置，字段与奖品接口一致。
    *   `free_refresh_left`: 剩余免费刷新次数。
    *   `diamond_cost`: 本次刷新实际扣除的钻石数量。
    *   `diamond`: 刷新后用户最新的钻石余额。
*   **后端过滤规则**:
    每次刷新产生的奖池满足四大铁律：
    1.  必含当前配置最高价的礼物。
    2.  必含当前配置最低价的礼物。
    3.  必含一个“藏宝图”位。
    4.  必含一个“宝石碎片”位。
    *(注：后台若未配置正式的藏宝图/宝石碎片 ID，后端自动使用现有道具进行兜底)*

### 2. 玩法1：高级兑换配置列表 (新增 | 2026-06-18)
*   **接口 URL**: `/api/emo/lottery/exchange_config` (POST)
*   **请求参数**:
    *   `type_id` (amp;是, int): 抽奖盘/玩法 ID
*   **响应示例**:
    ```json
    {
      "code": 1,
      "msg": "获取成功",
      "data": [
        {
          "exchange_id": 3000244,
          "type_id": 3,
          "tab_name": "兑换项1",
          "gift_id": 244,
          "gift": {
            "id": 244,
            "name": "月貌花容",
            "pic": "https://...png",
            "price": 52000000
          },
          "gem_gift_id": 107,
          "gem_cost": 1,
          "card_gift_id": 107,
          "success_rate": 10,
          "is_virtual": 1
        }
      ]
    }
    ```
*   **前端重点字段**:
    *   `exchange_id`：兑换配置项唯一 ID，调用 `exchange_gift` 兑换执行接口时必须传入该参数。
    *   `tab_name`：页签/兑换项标题（如“星辰/皓月”等）。
    *   `gift`：目标兑换的终极高级礼物。
    *   `gem_cost`：兑换需要消耗的宝石碎片数量。
    *   `success_rate`：单张藏宝图增加的兑换成功率百分比（例如 `10` 表示投入一张图增加 `10%` 成功率）。
    *   `is_virtual`：`1` 表示当前为联调兜底数据，正式上线后需由运营后台配置正式项并返回 `0`。

### 3. 玩法1：藏宝图高级兑换 (调整 | 2026-06-18)
*   **接口 URL**: `/api/emo/lottery/exchange_gift` (POST)
*   **请求参数**:
    *   `exchange_id` (是, int): 兑换配置项 ID（来自 `exchange_config` 接口获取的对应值）
    *   `card_count` (是, int): 本次投入消耗的藏宝图数量，可以为 `0`
*   **向后兼容参数**:
    *   `type_id` (老参数，新接入不再推荐)
    *   `config_id` (老兼容参数，等同 `exchange_id`)
    *   `gift_id` (老兼容参数，不推荐新接入)
*   **响应示例 (兑换失败)**: 
    ```json
    {
      "code": 0,
      "msg": "兑换失败",
      "data": {
        "exchange_id": 3000244,
        "success": false,
        "success_rate": 0,
        "gift": null,
        "remain_card_count": 0,
        "remain_gem_count": 0
      }
    }
    ```
*   **响应示例 (兑换成功)**: 
    ```json
    {
      "code": 1,
      "msg": "兑换完成",
      "data": {
        "exchange_id": 3000244,
        "success": true,
        "success_rate": 45,
        "gift": {
          "id": 244,
          "name": "月貌花容",
          "pic": "https://...png",
          "price": 52000000
        },
        "remain_card_count": 2,
        "remain_gem_count": 10
      }
    }
    ```
*   **后端结算规则**:
    1. 优先消耗用户的宝石碎片；
    2. 若 `card_count > 0`，再消耗对应张数的藏宝图；
    3. **零藏宝图必败**：若 `card_count == 0`，成功率强制为 0%，必定兑换失败，但仍然会消耗扣除宝石碎片。
    4. 成功率计算公式：成功率 = min(100, 单张成功率 * card_count)。
    5. 兑换成功时：发放目标高级礼物至背包，返回 `success=true`。
    6. 兑换失败时：只扣除消耗 of 碎片/藏宝图，不发放目标礼物，返回 `success=false`。

---

## 🛠️ 四、 前端推荐接入逻辑时序

1. **页面初始化**:
   * 调用 `/api/emo/user/getMoney` 获取用户当前的钻石余额与钥匙（代币）余额。 
   * 调用 `/api/emo/lottery/get_room_detail` 获取玩法配置（包含抽奖按钮档位）。 
   * 调用 `/api/emo/lottery/get_prizes` 获取奖池里的所有礼物基础列表。 
2. **刷新奖池（玩法 1 特有）**:
   * 手动刷新时调用 `/api/emo/lottery/refresh_pool`，用返回的 18 格 `list` 覆盖本地转盘对应的展示元素。 
3. **点击抽奖**:
   * 点击单抽/多抽时，客户端**锁死抽奖按钮**（通过 `isAnimating` 加锁，阻断连点）。 
   * 发起请求 `/api/emo/lottery/draw`。 
   * 拿到返回值后，通过 `total_value` 准备结算结果，并通过最高价礼物的 ID 找到转盘对应的格子索引，驱动跑马灯动效旋转停靠至该格子。 
   * 动效播放结束，打开结算弹窗，同时**解锁抽奖按钮**。 
4. **兑换与运势**:
   * 运势页面通过获取 `/api/fortune/getLotteryList` 进行渲染。 
   * 用户通过钻石购买钥匙时调用 `/api/emo/user/diamondChangeLotteryCoin`，并同步更新余额。 
   * **高级兑换**：先调用 `/api/emo/lottery/exchange_config` 获取最新的配置项与 `exchange_id`，点击兑换时使用最新 `/api/emo/lottery/exchange_gift` 接口完成兑换。
