# 🍎 iOS 端原生游戏页面切图布局与流转全景对照指南

本指南作为 iOS 端原生开发（Objective-C & Masonry 架构）的**像素级对齐手册**，完美融合并参考了 [iOS端原生游戏移植分步实施规范指南.md](file:///home/pantehr/Downloads/项目/双端交流/iOS端原生游戏移植分步实施规范指南.md) 中已通过审计的细节共识。

所有布局基准均以 **iPhone 标称逻辑分辨率 375 * 667 pt** 为缩放底座，开发时须使用 `Masonry` 做自适应约束适配。

---

## 🧱 第一阶段：游戏大入口与列表选择弹窗

### 📺 页面 0.1：多游戏选择列表弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomNativeGameView` (继承自宿主居中弹窗基类 `CenterPopupView`)
  * 弹窗底板大小：宽 **`297` pt**，高 **`393` pt**。
  * 屏幕位置：在全屏屏幕中央垂直水平居中，背景自带不透明度为 `0.5` 的黑色半透明遮罩。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`youxi_dialog_bg.png`
     * 控件大小：宽 `297` pt，高 `393` pt。
     * 相对位置：Masonry 撑满弹窗边缘（`make.edges.equalTo(self);`）。
  2. **右上角关闭按钮**：
     * 切图名：`img_cancal.png` (红色X图标)
     * 控件大小：宽 **`24` pt**，高 **`24` pt**。
     * 相对位置：距底板顶部 **`14` pt**，距底板右边缘 **`14` pt**。
  3. **标题 Label**：
     * 文字：“抽奖盘”，字号 `15` pt 粗体，颜色 `#FFFFFF`，水平居中。
     * 相对位置：距底板顶部 `16` pt，水平居中。
  4. **游戏列表容器 (UICollectionView)**：
     * 网格配置：3列 × 3行，共预留 7 个格子。
     * 布局间距：单个 Cell 宽 **`80` pt**，高 **`110` pt**。最小列间距 `8` pt，最小行间距 `12` pt。
     * 容器约束：距弹窗顶部 **`64` pt**，距左右边缘各 **`24` pt**，距底部 `20` pt。
  5. **玩法 1（寻梦之旅）入口预览图**：
     * 切图名：`native_game_one_preview.png`
     * 控件大小：宽 `72` pt，高 `72` pt，居中偏上，带 `4` pt 圆角。
     * 相对位置：CollectionView 中的第 1 个 Cell 内部。
  6. **玩法 2（神木栖灵）入口预览图**：
     * 切图名：`theme_game_two_entry_icon.png`
     * 控件大小：宽 `72` pt，高 `72` pt，居中偏上，带 `4` pt 圆角。
     * 相对位置：CollectionView 中的第 2 个 Cell 内部。
  7. **敬请期待占位格预览图**：
     * 切图名：`chat_room_plate_draw.png` (使用此底座，文字显示“敬请期待”，颜色为 `#9E9E9E`)
     * 控件大小：宽 `72` pt，高 `72` pt。
     * 相对位置：CollectionView 中的第 3 ~ 7 个 Cell 内部。
  8. **单个 Cell 底部玩法名称 (nameLabel)**：
     * 字体大小 `11` pt，颜色 `#E1F5FE`，居中对齐，距上部预览图底部 `6` pt。
* **【页面流转与跳转关系】**
  * **如何进入**：点击语聊房主页面悬浮按钮 `chat_room_voice_draw_circle_icon` -> 弹出本列表。
  * **如何退出**：点击右上角红色关闭按钮，或者点击弹窗周围半透明阴影遮罩 -> 弹窗执行 Dismiss 缩小淡出动画。
  * **页面内跳转**：
    * 点击 **第一格（玩法1）** -> 列表弹窗自动 Dismiss，同时在语聊房最上层拉起 `MLChatRoomThemeGameOneView`；
    * 点击 **第二格（玩法2）** -> 列表弹窗自动 Dismiss，同时在语聊房最上层拉起 `MLChatRoomThemeGameTwoView`；
    * 点击 **“敬请期待”格子** -> 拦截点击事件，直接忽略，不作任何处理（`enabled = NO`）。

---

## ⚔️ 第二阶段：玩法 1（寻梦之旅）页面全景规范

### 📺 页面 1.1：玩法 1 游戏主页面
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameOneView`
  * 视图大小：宽 **`375` pt**，高 **`667` pt** (全屏贴底)。
  * 屏幕位置：全屏拉伸贴底，遮盖在语聊房最上层。
* **【页面内切图资产、尺寸与位置】**
  1. **主背景图**：
     * 切图名：`theme_game_one_clean_bg.png`
     * 控件大小：宽 `375` pt，高 `667` pt，设置 `AspectFill` 贴底。
  2. **规则按钮** 与 **记录按钮**：
     * 切图名：`theme_game_one_rule_btn_brighter.png` / `theme_game_one_record_btn_brighter.png`
     * 控件大小：宽 `36` pt，高 `36` pt。
     * 相对位置：距背景顶部 `16` pt，距右边缘 `16` pt，两按钮水平间距 `10` pt。
  3. **今日运势悬浮条**：
     * 控件大小：宽 `74` pt，高 `23` pt。
     * 相对位置：水平悬浮在规则/记录按钮组的左侧 `10` pt 处（代码手搓 40% 半透明黑底，圆角 `11.5` pt）。
  4. **钻石栏** 与 **钥匙栏**：
     * 控件大小：宽 `90` pt，高 `22` pt（代码手搓 40% 半透明黑底，圆角 `11` pt）。
     * 相对位置：钻石距左边缘 `24` pt，钥匙距右边缘 `24` pt，距顶部 `50` pt。
     * 附着图标：钻石图 `theme_game_one_cover_diamond.png` (`30*30` pt 挂左)；钥匙图 `theme_game_one_cover_key.png` (`30*30` pt)；加号按钮 `theme_game_one_plus_icon.png` (`22*22` pt 挂右)。
  5. **18 宫格礼物卡片 (环形排布)**：
     * 控件大小：单个卡片固定宽 **`46` pt**，高 **`60` pt**。整个 18 宫格区域距顶部固定 **`140` pt**，水平居中。
     * 排布间距：顶部横排 6 个（格子 1~6，水平间距 `8` pt），右侧竖排 2 个（格子 7~8，垂直间距 `6` pt），底部横排 6 个（间距 `8` pt），左侧竖排 2 个（间距 `6` pt）。
     * 底盘图片：`theme_game_one_gift_board_1/2/3/4.png` (交替作为格子底层背景)。
     * 礼物图标：网络加载，居中叠加展示，带 **`4` pt 的内边距**。
     * 卡片名字条：高 `14` pt，半透明黑色背景，字号 `9.5` pt 粗体，贴卡片底部。
  6. **中间大宝箱**：
     * 切图名：`theme_game_one_cover_box.png`
     * 控件大小：宽 **`100` pt**，高 **`100` pt**。
     * 相对位置：位于 18格大环中央正中间，垂直水平居中。
  7. **寻梦值进度条**：
     * 切图名：`theme_game_one_dream_bar.png` (作为进度条底座)
     * 控件大小：宽 **`120` pt**，高 **`12` pt**。
     * 相对位置：置于中间大宝箱正下方 **`8` pt**。右侧跟随展示 `currentLucky/200` 文本（字号 `10` pt，金黄色）。
  8. **刷新奖池按钮**：
     * 控件大小：宽 `90` pt，高 `32` pt。
     * 相对位置：距左边缘 `24` pt，距底部 `120` pt。
     * 资源拼装：按钮图标使用 `theme_game_one_refresh_btn.png`，底座使用蓝条 `theme_game_one_refresh_free_bar_origin.png` (免费时可叠加 `theme_game_one_refresh_free.png`)。
  9. **藏宝图兑换按钮**：
     * 切图名：`theme_game_one_exchange_btn.png`
     * 控件大小：宽 `90` pt，高 `32` pt。
     * 相对位置：距右边缘 `24` pt，距底部 `120` pt。
  10. **三档抽奖按钮组 (一连/十连/百连)**：
      * 切图名：`theme_game_one_draw_one/ten/hundred.png`
      * 控件大小：单按钮宽 **`112` pt**，高 **`56` pt**。
      * 相对位置：呈倒品字形排布。十连抽居中置顶；一连抽和百连抽在左右两侧偏下方，两两间距 `12` pt。底部避让 Safe Area 安全区 **`24` pt**。
* **【页面流转与跳转关系】**
  * **如何进入**：多游戏大入口 `MLChatRoomNativeGameView` 点击玩法 1 触发唤起。
  * **如何退出**：点击左上角返回按钮 -> 销毁移除当前页面。
  * **页面内跳转**：
    * 点击 规则/历史记录 按钮 -> 居中弹出 `MLChatRoomThemeGameOneRuleView` / `RecordView`；
    * 点击 钥匙余额栏的加号 -> 居中弹出 `MLChatRoomThemeGameOnePurchaseView`；
    * 点击 藏宝图兑换按钮 -> 居中弹出 `MLChatRoomThemeGameOneExchangeView`；
    * 抽奖跑马灯及 SVGA 特效播放完毕 -> 居中弹出 `MLChatRoomThemeGameOneResultView`。

---

### 📺 页面 1.2：玩法 1 钥匙购买弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameOnePurchaseView` (居中弹窗)
  * 弹窗底板大小：宽 **`315` pt**，高 **`360` pt**。
  * 屏幕位置：全屏正中央垂直水平居中，背景带 `0.5` 黑色半透明蒙层。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`theme_game_one_purchase_popup_board.png`
     * 控件大小：宽 `315` pt，高 `360` pt。
     * 相对位置：Masonry 撑满弹窗。
  2. **右上角关闭按钮**：
     * 切图名：`theme_game_one_purchase_back.png`
     * 控件大小：宽 **`36` pt**，高 **`36` pt**。
     * 相对位置：距底板顶部 **`16` pt**，距底板右边缘 **`16` pt**。
  3. **顶部余额指示栏 (钻石/钥匙)**：
     * 钻石底座与图标：`theme_game_one_purchase_diamond_bar/icon.png`；钥匙底座与图标：`theme_game_one_purchase_key_bar/icon.png`。
     * 控件大小：高 `22` pt，左右并排。
     * 相对位置：距底板顶部 **`50` pt**，距左右边缘各 **`24` pt**。
  4. **中部商品展示框组**：
     * 左侧钥匙展示背板：`theme_game_one_purchase_board_left.png` (宽 `97`，高 `100` pt)
     * 右侧礼物赠送背板：`theme_game_one_purchase_board_gift.png` (宽 `97`，高 `100` pt)
     * 中间加号：`theme_game_one_purchase_plus_origin.png` (大小 `20 * 20` pt)
     * 相对位置：整组商品框距底板顶部 **`94` pt**，水平居中。
  5. **4档快捷选择按钮组 (1 / 10 / 100 / 其它)**：
     * 切图名：`theme_game_one_purchase_one/ten/hundred/other_selected.png` (及对应 normal 图)
     * 控件大小：单按钮宽 **`60` pt**，高 **`24` pt**，水平间距 `8` pt。
     * 相对位置：一横排平铺，距底板顶部 **`220` pt**，水平居中。
  6. **自定义输入框 (UITextField)**：
     * 控件大小：宽 `180` pt，高 `32` pt。
     * 相对位置：位于快捷档位按钮正下方 `10` pt，水平居中，默认处于 `hidden = YES`。
  7. **确认购买按钮**：
     * 切图名：`theme_game_one_purchase_confirm.png`
     * 控件大小：宽 **`208` pt**，高 **`42` pt**。
     * 相对位置：距底板底端 **`24` pt**，水平居中。
* **【页面流转与跳转关系】**
  * **如何进入**：主界面点击钥匙资产栏加号，或抽奖时发现代币余额不足 -> 弹出本弹窗。
  * **如何退出**：点击右上角关闭按钮，或者点击外围半透明蒙层 -> 移除本弹窗。
  * **页面内跳转**：点击确认购买调用 `/buy_keys`；接口成功后**禁止关闭弹窗**，在本地扣除钻石并增加钥匙，刷新弹窗顶部资源栏数字，并回调更新主页面余额，支持快捷连续购买。

---

### 📺 页面 1.3：玩法 1 藏宝图高级兑换弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameOneExchangeView` (居中弹窗)
  * 弹窗底板大小：宽 **`344` pt**，高 **`528` pt**。
  * 屏幕位置：全屏正中央垂直水平居中，背景带 `0.5` 黑色半透明蒙层。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`theme_game_one_exchange_popup_board.png` (采用 AspectFit 填充，禁止拉伸)
     * 控件大小：宽 `344` pt，高 `528` pt。
  2. **倒计时时间图标**：
     * 切图名：`theme_game_one_exchange_time.png`
     * 控件大小：宽 `28` pt，高 `34` pt。
     * 相对位置：距底板顶部 **`21` pt**，距底板右边缘 **`30` pt**。
  3. **5页签选择栏 (星辰/皓月/银河/荣耀/传奇)**：
     * 切图名：`theme_game_one_exchange_tab_star/moon/galaxy/glory/legend_selected.png` (及对应 normal 图)
     * 控件大小：单个页签宽 **`54` pt**，高 **`23` pt**。
     * 相对位置：距底板顶部 **`74` pt**，各页签左起点为 `18`/`82`/`146`/`210`/`274` pt。
  4. **终极奖品礼物大外框**：
     * 切图名：`theme_game_one_exchange_gift_large.png`
     * 控件大小：宽 **`106` pt**，高 **`104` pt**。
     * 相对位置：距底板顶部 **`126` pt**，水平居中。内藏礼物图片宽 `56`，高 `56` pt，居中偏上（距大框顶 `14` pt）。
  5. **高级底座**：
     * 切图名：`theme_game_one_exchange_board.png`
     * 控件大小：宽 **`284` pt**，高 **`133` pt**。
     * 相对位置：距底板顶部 **`238` pt**，水平居中。
  6. **左右消耗小框**：
     * 切图名：`theme_game_one_exchange_gift_small.png`
     * 控件大小：宽 **`64` pt**，高 **`65` pt**，叠在高级底座上面。
     * 相对位置：距底板顶部 **`270` pt**。左小框距左边缘 `68` pt，右小框距右边缘 `68` pt。
     * 小框图标：左侧钻石碎片 `_cover_diamond` (`30*30` pt，距小框顶 `8` pt)；右侧藏宝图 `_cover_box` (`30*30` pt，距小框顶 `8` pt)。
  7. **中间加号**：
     * 切图名：`theme_game_one_exchange_plus.png`
     * 控件大小：宽 `19` pt，高 `19` pt。
     * 相对位置：距底板顶部 **`292` pt**，水平居中。
  8. **确认兑换大按钮**：
     * 切图名：`theme_game_one_exchange_confirm.png`
     * 控件大小：宽 **`244` pt**，高 **`49` pt**。
     * 相对位置：距底板顶部 **`444` pt**，水平居中。
* **【页面流转与跳转关系】**
  * **如何进入**：主界面点击“高级兑换” -> 弹出本弹窗。
  * **如何退出**：**本弹窗在 UI 布局设计上不设物理关闭按钮**。只能通过点击大底板 344*528 以外的半透明蒙层区域触发 Dismiss 关闭。
  * **页面内跳转**：点击页签切换兑换配置；点击藏宝图小框循环选择投入数量（0 -> 1 -> 2 -> ... -> 0）；当投入藏宝图数量为 `0` 时，点击开启兑换大按钮会被本地直接强行拦截并 Toast 提示。

---

### 📺 页面 1.4：玩法 1 中奖结果展示弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameOneResultView` (居中弹窗)
  * 弹窗底板大小：宽 **`315` pt**，高 **`390` pt**。
  * 屏幕位置：全屏正中央垂直水平居中，背景带 `0.5` 黑色半透明蒙层。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`theme_game_one_result_board.png`
     * 控件大小：宽 `315` pt，高 `390` pt。
  2. **右上角关闭按钮**：
     * 切图名：`theme_game_one_result_close.png`
     * 控件大小：宽 **`36` pt**，高 **`36` pt**。
     * 相对位置：距底板顶部 **`12` pt**，距底板右边缘 **`12` pt** (紧密贴最右上角)。
  3. **奖品滚动展示容器 (UIScrollView / UITableView)**：
     * 控件大小：宽 **`267` pt**，高 **`200` pt**。
     * 相对位置：距底板顶部 **`70` pt**，距底板底部 **`270` pt**，水平居中。
     * 列表 Cell 布局：横向排开，礼物小图 `32*32` pt，名称字号 `12` pt，价值 `11` pt 黄字显示“X钻石”，数量 `11` pt 蓝字显示“xN”。
  4. **再寻一次按钮**：
     * 控件背景图：`theme_game_one_purchase_confirm.png`
     * 控件大小：宽 **`80` pt**，高 **`22` pt**。
     * 相对位置：距底板顶部 **`300` pt**，水平居中。字号 `10` pt，金黄色粗体。
  5. **次数提示底版**：
     * 切图名：`theme_game_one_result_tag_bg.png`
     * 控件大小：宽 **`120` pt**，高 **`19` pt**。
     * 相对位置：距底板顶部 **`340` pt**，水平居中。内装“寻梦X次”文字。
* **【页面流转与跳转关系】**
  * **如何进入**：抽奖结束且跑马灯与 SVGA 特效播放完毕 -> 自动弹出本结果页。
  * **如何退出**：点击右上角关闭按钮，或者点击外围半透明蒙层 -> 移除本弹窗，主页面在 `onDismiss()` 时**延迟发起 `/getMoney` 接口请求同步钻石与钥匙资产**。
  * **页面内跳转**：点击“再寻一次”按钮 -> **立刻 Dismiss 关闭本弹窗，主页面直接发起下一次抽奖接口请求**（跳过余额更新），保证连抽体验。

---

### 📺 页面 1.5：玩法 1 规则及历史记录弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameOneRuleView` 与 `MLChatRoomThemeGameOneRecordView` (居中弹窗)
  * 弹窗底板大小：宽 **`315` pt**，高 **`390` pt**。
  * 屏幕位置：全屏正中央垂直水平居中，背景带 `0.5` 黑色半透明蒙层。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`theme_game_one_rule_popup.png` (规则页) / `theme_game_one_record_bg.png` (记录页)
     * 控件大小：宽 `315` pt，高 `390` pt。
  2. **专属关闭按钮**：
     * 切图名：`theme_game_one_rule_back.png` (规则) / `theme_game_one_record_back.png` (记录)
     * 控件大小：宽 `36` pt，高 `36` pt。
     * 相对位置：放置在弹窗的**最左上角**。
  3. **文本展示区 (规则页 UITextView)**：
     * 控件大小：宽 **`267` pt**，高 **`280` pt**。
     * 相对位置：距底板顶部 **`60` pt**，水平居中。`editable = NO`，字号 `12` pt，颜色 `#E1F5FE`。
  4. **页签按钮组 (记录页专用)**：
     * 按钮素材：全服 `theme_game_one_record_tab_all_selected/normal.png`；我的 `_tab_mine_selected/normal_equal.png`。
     * 控件大小：单个宽 **`100` pt**，高 **`28` pt**。
     * 相对位置：距底板顶部 **`40` pt**，水平居中并排。
  5. **记录列表容器 (记录页 UITableView)**：
     * 控件大小：宽 **`267` pt**，高 **`280` pt**。
     * 相对位置：距底板顶部 **`80` pt**，水平居中。单个 Cell 高度固定为 **`36` pt**。
* **【页面流转与跳转关系】**
  * **如何进入**：主界面点击规则按钮 -> 弹出规则页；点击记录按钮 -> 弹出记录页。
  * **如何退出**：点击左上角返回按钮，或者点击外围半透明蒙层 -> 移除弹窗。
  * **页面内交互逻辑**：
    * 记录页签切换分别拉取全服和个人历史接口；
    * **全网用户隐私脱敏**：全服记录里的昵称若长度 >= 2 汉字，必须做 `首字 + **` 脱敏处理（例如“李小明” -> “李\*\*”）；
    * **脏数据空行过滤**： cellForRow 数据源绑定中若有任何核心字段（中奖时间、礼物名）为 nil 或空字符，**直接抛弃过滤该条数据**，防止列表出现白行。

---

## 🌲 第三阶段：玩法 2（神木栖灵）页面全景规范

### 📺 页面 2.1：玩法 2 游戏主页面
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameTwoView`
  * 视图大小：宽 **`375` pt**，高 **`667` pt** (全屏贴底)。
  * 屏幕位置：盖在语聊房最上层，全屏拉伸贴底。
* **【页面内切图资产、尺寸与位置】**
  1. **主背景图**：
     * 切图名：`theme_game_two_clean_bg.png`
     * 控件大小：宽 `375` pt，高 `667` pt，设置 `AspectFill` 贴底。
  2. **规则按钮** 与 **记录按钮**：
     * 切图名：`theme_game_two_rule_btn.png` / `theme_game_two_record_btn.png`
     * 控件大小：宽 `36` pt，高 `36` pt。
     * 相对位置：距背景顶部 `16` pt，距右边缘 `16` pt，两按钮水平间距 `10` pt。
  3. **今日运势悬浮条**：
     * 控件大小：宽 `74` pt，高 `23` pt，水平悬浮在规则/记录按钮组的左侧 `10` pt（代码手搓 40% 半透明黑底，圆角 `11.5` pt）。
  4. **资产显示栏 (钻石与祝灵珠)**：
     * 控件大小：高度 **`32` pt**，距顶部 **`50` pt**，左右边缘 **`18` pt**。为干净透明的线性布局。
     * 附着图标：钻石 `theme_game_two_diamond_icon.png` (`32*32` pt)；祝灵珠 **`theme_game_one_purchase_key_icon.png`** (`30*30` pt，跨玩法复用钥匙图)；加号 `theme_game_two_plus_icon.png` (`28*28` pt)。
  5. **9 个灵果发光位置卡片 (绝对定位)**：
     * 控件大小：单卡片宽高固定为 **`42 * 42` pt**。
     * 发光光圈：`theme_game_two_center_fruit.png` (最底层)；底盘：`theme_game_two_center_frame.png` (中层)。礼物小图带 **`5` pt 内边距** 居中叠在最上层。
     * **9个果实的 Center (X, Y) 绝对坐标**（依据 375 * 667 pt 比例对齐）：
       * 灵果 1：`(150, 185)` pt ─ 灵果 2：`(105, 235)` pt ─ 灵果 3：`(195, 235)` pt
       * 灵果 4：`(70, 290)` pt  ─ 灵果 5：`(148, 280)` pt ─ 灵果 6：`(230, 290)` pt
       * 灵果 7：`(100, 350)` pt ─ 灵果 8：`(200, 350)` pt ─ 灵果 9：`(148, 395)` pt
  6. **三档抽奖按钮组 (一连/十连/百连)**：
     * 切图名：`theme_game_two_draw1_btn.png` / `_draw10_btn.png` / `_draw100_btn.png`
     * 控件大小：单个宽 **`104` pt**，高 **`60` pt**。
     * 相对位置：水平一排平铺底端。间距 `10` pt，底端避让 Safe Area 安全区 **`14` pt**，整组水平居中。
* **【页面流转与跳转关系】**
  * **如何进入**：多游戏大入口 `MLChatRoomNativeGameView` 点击玩法 2 触发唤起。
  * **如何退出**：点击左上角返回按钮，或者向下滑动 Dismiss 页面。
  * **页面内跳转与动效**：
    * 点击 规则/历史记录 -> 弹出 `MLChatRoomThemeGameTwoRuleView` / `RecordView`；
    * 点击 祝灵珠栏加号 -> 弹出 `MLChatRoomThemeGameTwoPurchaseView`；
    * **错峰物理悬浮动效**：9个灵果初始化后挂载缓动代码，每个使用错开的延迟（`delay = index * 0.15`）在 Y 轴上垂直上下微弱飘移（位移 `2.5` pt，时间在 `1.8 ~ 2.2` 秒间，配置 `Repeat` 与 `Autoreverse`）；
    * 点击 1/10/100 抽 -> 乐观扣费 -> 播放全屏 `theme_game_two_harvest.svga` 动效并伴随 9 个果实交替 Alpha 闪烁 -> 特效播完弹出 `MLChatRoomThemeGameTwoResultView`。

---

### 📺 页面 2.2：玩法 2 祝灵珠购买弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameTwoPurchaseView` (居中弹窗)
  * 弹窗底板大小：宽 **`315` pt**，高 **`360` pt**。
  * 屏幕位置：全屏正中央垂直水平居中，背景带 `0.5` 黑色半透明蒙层。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`theme_game_two_purchase_bg.png`
     * 控件大小：宽 `315` pt，高 `360` pt。
  2. **顶部标题框**：
     * 切图名：`theme_game_two_purchase_title.png`
     * 控件大小：宽 `315` pt，高 `360` pt (叠在背景图上方)。
  3. **关闭/返回按钮**：
     * 切图名：`theme_game_two_purchase_back.png`
     * 控件大小：宽 `36` pt，高 `36` pt，距顶 `16` pt，距右 `16` pt。
  4. **顶部余额指示栏 (钻石/祝灵珠)**：
     * 控件大小：高度 `22` pt，左右并排，距顶 `50` pt，左右边距各 `24` pt (代码手搓半透明圆角矩形)。
  5. **商品展示框**：
     * 左侧代币背板：`theme_game_two_purchase_frame_left.png` (宽 `97`，高 `100` pt)
     * 右侧礼物背板：`theme_game_two_purchase_frame_right.png` (宽 `97`，高 `100` pt)
     * 中间加号：`theme_game_two_plus_icon.png` (大小 `20 * 20` pt)
     * 相对位置：整组商品卡片距弹窗顶部 **`94` pt**，水平居中。
  6. **4档快捷选择按钮组 (1 / 10 / 100 / 其它)**：
     * 切图名：直接使用成品切图 `theme_game_two_purchase_one/ten/hundred/other.png`
     * 控件大小：单按钮宽 **`60` pt**，高 **`24` pt**，按钮水平间距 `8` pt。
     * 相对位置：一横排平铺，距顶 **`220` pt**，水平居中。
  7. **自定义输入框 (UITextField)**：
     * 控件大小：宽 `180` pt，高 `32` pt，位于快捷按钮下方 `10` pt 处，默认 `hidden = YES`。
  8. **确认购买按钮**：
     * 切图名：`theme_game_two_purchase_confirm.png`
     * 控件大小：宽 **`208` pt**，高 **`42` pt**。
     * 相对位置：距底端 **`24` pt**，水平居中。
* **【页面流转与跳转关系】**
  * **如何进入**：主界面点击祝灵珠余额栏加号，或抽奖时发现余额不足 -> 弹出本弹窗。
  * **如何退出**：点击右上角关闭按钮，或者点击外围半透明蒙层 -> 移除本弹窗。
  * **页面内跳转**：接口购买成功后**禁止关闭弹窗**，在本地进行扣除与刷新，并局部刷新弹窗顶部资源余额栏，支持连续购买。

---

### 📺 页面 2.3：玩法 2 中奖结果展示弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameTwoResultView` (居中弹窗)
  * 弹窗底板大小：宽 **`315` pt**，高 **`390` pt**。
  * 屏幕位置：全屏正中央垂直水平居中，背景带 `0.5` 黑色半透明蒙层。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`theme_game_two_result_bg.png`
     * 控件大小：宽 `315` pt，高 `390` pt。
  2. **右上角关闭按钮**：
     * 切图名：`theme_game_two_purchase_back.png` (大小 `36 * 36` pt，距顶 `12` pt，距右 `12` pt)。
  3. **奖品滚动展示容器 (UIScrollView / UITableView)**：
     * 控件大小：宽 `267` pt，高 `200` pt，距顶 `70` pt 居中。
     * 列表展示：礼物名称字号 `12` pt (古木深棕色 `#7A4318`)；价值 `11` pt (橙褐色 `#C1631E`)；数量 `11` pt (红褐色 `#A74F1F`)。
  4. **再祝灵一次按钮**：
     * 控件大小：宽 **`80` pt**，高 **`22` pt**。
     * 相对位置：距底板顶部 **`300` pt**，水平居中 (无物理切图，代码手搓木色/金色圆角背景底板)。
  5. **次数提示标签 (核心切图切换)**：
     * 控件大小：宽 **`120` pt**，高 **`20` pt**，位于再祝灵一次按钮下方 `340` pt 处。
     * 渲染规则：**严禁拼接字符串！**直接根据抽奖次数（1 / 10 / 100）切换对应的成品图片：
       * 1抽使用：`theme_game_two_result_tag_one.png`
       * 10抽使用：`theme_game_two_result_tag_ten.png`
       * 100抽使用：`theme_game_two_result_tag_hundred.png`
* **【页面流转与跳转关系】**
  * **如何进入**：祝灵采摘动效及果实闪烁播放完毕 -> 弹出结果页。
  * **如何退出**：点击关闭按钮，或者点击外围半透明蒙层 -> 移除当前页，主页面在 `onDismiss()` 中**延迟同步 `/getMoney` 接口**。
  * **页面内跳转**：点击“再祝灵一次”按钮 -> **立刻 Dismiss 移除当前弹窗，主页面直接发起下一次抽奖请求**，实现秒连抽。

---

### 📺 页面 2.4：玩法 2 规则及历史记录弹窗
* **【页面大小与屏幕位置】**
  * 页面组件类：`MLChatRoomThemeGameTwoRuleView` 与 `MLChatRoomThemeGameTwoRecordView` (居中弹窗)
  * 弹窗底板大小：宽 **`315` pt**，高 **`390` pt**。
  * 屏幕位置：全屏正中央垂直水平居中，背景带 `0.5` 黑色半透明蒙层。
* **【页面内切图资产、尺寸与位置】**
  1. **背景底板**：
     * 切图名：`theme_game_two_rule_clean.png` (规则页) / `theme_game_two_record_clean.png` (记录页)
     * 控件大小：宽 `315` pt，高 `390` pt。
  2. **专属关闭按钮**：
     * 切图名：`theme_game_two_rule_back.png` (规则) / `theme_game_two_record_back.png` (记录)
     * 控件大小：宽 `36` pt，高 `36` pt，叠在最左上角。
  3. **文本展示区 (规则页 UITextView)**：
     * 控件大小：宽 `267` pt，高 `280` pt，距顶部 `60` pt。字号 `12` pt 细体，文本颜色采用木质系暖白。
  4. **页签按钮组 (记录页专用)**：
     * 按钮素材：全服 `theme_game_two_record_tab_all_selected/normal.png`；我的 `_tab_mine_selected/normal.png` (宽高均 `100 * 28` pt)。
     * 相对位置：距底板顶部 `40` pt，水平并排居中。
  5. **历史记录列表容器 (记录页 UITableView)**：
     * 控件大小：宽 `267` pt，高 `280` pt，距顶 `80` pt 居中。单个 Cell 高度固定为 `36` pt。
* **【页面流转与跳转关系】**
  * **如何进入**：主界面点击规则按钮 -> 弹出规则页；点击记录按钮 -> 弹出记录页。
  * **如何退出**：点击左上角返回/关闭按钮，或者点击外围半透明蒙层 -> 移除弹窗。
  * **页面内交互逻辑**：
    * 记录页签切换分别拉取玩法2对应接口（`lottery_id=2`）；
    * **全网用户隐私脱敏**：全服记录里对用户昵称做 `首字 + **` 脱敏处理（例如“王大锤” -> “王\*\*”）；
    * **脏数据过滤**：cellForRow 中过滤抛弃任何核心字段为空的条目，防止列表出现白行。
